import 'package:flutter/material.dart';

class ToastField extends StatelessWidget {
  final String? meassage;
  const ToastField({super.key, required this.meassage});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
